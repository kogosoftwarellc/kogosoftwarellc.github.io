#!/bin/bash

TMP_DIR="/tmp/`date +%s`"

echo "Current directory: $PWD"
echo "Temp directory $TMP_DIR"

exit

if [[ -d "$TMP_DIR" ]]; then
  rm -rf "$TMP_DIR"
fi

npm install

gulp

mv dist $TMP_DIR

git checkout master

mv "$TMP_DIR/*" .
